__pycache__/
*.py[cod]

# Фронт (Vite/React): зависимости НЕ коммитим, но собранный dist/ — коммитим,
# чтобы приложение запускалось офлайн без Node/npm (main.py раздаёт dist статикой).
frontend/node_modules/

# Офлайн-комплект Python-пакетов КОММИТИМ в git (для офлайн-установки после
# git clone). Но крупные wheel (>95 МБ) лежат кусками .partNNN, а сам
# реассемблированный .whl создаётся при установке (tools\reassemble_wheels.py)
# и в git не нужен. См. README.md (раздел про офлайн-установку).
offline_kit/wheels/torch-*.whl
offline_kit/wheels/paddlepaddle-*.whl

pdf_conversion.log
docling_conversion.log
docling_convert.log
conversion_state.json
pdf_to_convert/conversion_state.json
pdf_to_convert/*.pdf
converted_word_docling/
converted_word/
pdf_backup/
output/
inference_results/
log_output/
_paddle_tmp/
**/_paddle_tmp/

.venv/
venv/
env/
.idea/
.vscode/
.claude/
/CLAUDE.md


# dev-инструменты вёрстки (не коммитим)
_cache/
_dev.py
_render.py
_dev_*
_src_*
_out_*
_compare_*
tests/_*.py
# PDF-экспорты в корне (визуальная сверка результата) — временные
/*.pdf

# Модель LLM: собранный .gguf НЕ коммитим (>100 МБ, лимит GitHub) —
# в git лежат куски models/*.gguf.partNNN (<100 МБ), модель склеивается
# из них автоматически при первом запуске (см. llm_correct._resolve_model).
models/*.gguf

# Инструкция по вендорингу моделей — локальный референс, на GitHub не нужна
INSTALL_RAPIDOCR.md
