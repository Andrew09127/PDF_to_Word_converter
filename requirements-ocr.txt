paddleocr==2.10.0
# Install PaddlePaddle separately for your OS/Python version.
# Linux CPU example:
# python -m pip install paddlepaddle -i https://www.paddlepaddle.org.cn/packages/stable/cpu/

# --- Опциональная локальная LLM-доочистка (флаг --llm) ---
# Ставится с готовым CPU-wheel (без компиляции):
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# Модель .gguf кладётся в models/ (например qwen2.5-1.5b-instruct-q4_k_m.gguf).
llama-cpp-python>=0.3.0
